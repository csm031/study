window.onload = () => {
    let createButton = document.createElement("button");
    let buttonText = document.createTextNode('Click');
    createButton.innerHTML = `버튼1`;
    createButton.appendChild(buttonText);
    document.body.appendChild(createButton);
    document.body.ap
}